/* let fruit = prompt("¿Qué fruta comprar?", "apple");

let bag = {
    [fruit]: 5, // El nombre de la propiedad se obtiene de la variable fruit
};

alert(bag.apple); // 5 si fruit es="apple" */
let miObj = { nombre: 'Edgar', apellido: 'Vivar', edad: 35, 'codigo postal': 1071 }
alert(miObj.apellido)
for (x of miObj) { console.log(x) }
